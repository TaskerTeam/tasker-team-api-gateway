syntax = "proto3";

package task;

service TaskService {
    rpc Create(TaskRequest) returns (TaskResponse){}
    //rpc Update() returns (){}
}

message TaskRequest {
    string name = 1;
    double price = 2;
}

message TaskResponse {
    int32 id = 1;
    string name = 2;
    double price = 3;
}
